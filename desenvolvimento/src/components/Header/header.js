import styles from './header.module.css'
export const Header = () => {
    return(
        <section className={styles.exemplo}>
            <p>Seção header</p>

            <h1>Lembrem de adicionar essa seção no app.js</h1>
        </section>
    )
}